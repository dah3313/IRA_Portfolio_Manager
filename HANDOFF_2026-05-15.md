# IRAPM Phase 1 — Handoff 2026-05-15

## CRITICAL — cash_out STILL MISSING from /runs reports

The operator noted at end of session: the cash_out column in /runs
output is still empty. This is NOT a regression from tonight's work.
It is the original problem that motivated this entire architecture pass.

What this means for tomorrow:
- The /runs output is rendered by the OLD IPMS output package
  (ipms/output/*.py), which never derives cash flows correctly.
- The NEW event log we wired tonight contains the raw data needed
  (portfolio_snapshot cash_dollars per cycle), but cash flow attribution
  comes from fill_received events — which are emitted from action_layer.py,
  not yet wired.
- The NEW reporter (Phase 2) reads events.jsonl and computes
  cash_flow_in / cash_flow_out by aggregating fill_received events
  signed by side (BUY = cash out, SELL = cash in) within each period.

Order of operations to deliver cash_out to the operator:
1. Phase 1 Step 3a: action_layer.py emissions (order_placed,
   fill_received, withdrawal_executed, cb_transition, phase_transition)
   — fill_received is the LOAD-BEARING event for cash flow
2. Phase 1 Step 3b: annual_review.py + alerter.py emissions
3. Phase 2: build report.py per REPORT_DECISIONS.md, consume events.jsonl
4. Operator sees real cash_out values for the first time

Until step 4 lands, /runs reports remain visibly broken. That is by
design — we are not patching the old IPMS output. We are replacing it.

Do not get distracted by the broken /runs output tomorrow. The fix is
2-3 sessions away, on a known path.## Status: Phase 1 Step 2 complete. Baseline scenario passes.

20-year scenario `baseline_20yr_phase3_2016` ran end-to-end. Terminal AUM $3,304,781.68, Phase 3, CB1, no halts. All expectations passed. Same financial outcome as the pre-event-log baseline — confirms emissions are observationally additive.

## Files modified this session (all dropped into C:\portfolio\)

| File | Change |
|---|---|
| `event_log.py` | Phase 1 Step 1 — writer module + 13 helpers (delivered prior session) |
| `persistence.py` | Added `events_log()` accessor on Paths (delivered prior session) |
| `test_event_log.py` | 60 unit tests, all passing |
| `EVENT_LOG_SPEC.md` | Removed `cash_unsettled_dollars` (3 places); fixed `combined_token_state` to be object-not-string (2 places) |
| `decision_layer.py` | Added `CycleSnapshotData` dataclass + `cycle_snapshot` field on `DecisionOutput` + `_build_cycle_snapshot` helper; threaded `inputs` through `_finalize`; pause-skip path routed via `_finalize` with `force_skip_action_layer=True` |
| `clock.py` | Added `now_utc()` to Protocol + SystemClock + FrozenClock + AdvancingClock (all preserve existing naive `now()` contract) |
| `cycle.py` | 6 emission points in weekly cycle (cycle_started, decision_made, cycle_halted, cycle_completed, portfolio_snapshot, state_snapshot); 2 in daily-token cycle (cycle_started, cycle_completed); `_to_aware_utc` helper for timezone normalization |

## Test status

```
pytest test_annual_review_tz.py test_cycle_calendar.py test_event_log.py test_harness_bail_logic.py test_price_data_as_of.py test_sgov_refill_meaningful_drift.py test_translate_tsvs.py -v
```

109 passed.

## Integration bugs caught and fixed this session

All three followed the same pattern: assumed-not-verified field names.

1. **`Clock.now()` returns naive datetime, not tz-aware** — Required adding `now_utc()` to Clock seam. Fix in clock.py + cycle.py (use `_aware` variants for event log calls).

2. **`Position.quantity_shares` does not exist; the field is `Position.quantity`** — Fix in decision_layer.py `_build_cycle_snapshot`.

3. **`CombinedTokenState.value` does not exist; CombinedTokenState is a dataclass with four boolean fields, not an enum** — Fix in cycle.py decision_made payload. Spec also updated to show object form.

## What's next: Phase 1 Step 3 (next session)

Add emission points to four more files. The handoff order:

### `action_layer.py` — 5 emissions
- `order_placed` — in `_place_one_order`, after `broker.place_order()` returns successfully
- `fill_received` — in `_place_one_order`, one event per Fill in `OrderResult.fills` after FILLED status
- `withdrawal_executed` — in `_execute_withdrawal`, after all SELL legs complete
- `cb_transition` — in `_execute_cb_state_transition`, parallel to existing `append_cb_transition` to cb_transitions.jsonl (both writes during migration per spec §1.3)
- `phase_transition` — in `_execute_phase_transition`, after BUY leg completes
- Also: emit `state_snapshot` after every cb_transition and phase_transition (two of the three trigger sources not in cycle.py)

### `annual_review.py` — 1 emission + 1 trigger
- `annual_review_completed` — when annual review subroutine completes
- Emit `state_snapshot` after it (third trigger source)

### `alerter.py` — 1 emission
- `alert_emitted` — in `dispatch()`, after both channel attempts complete

### Same discipline as this session
- Audit attribute names against source files BEFORE writing diffs
- Show diff to operator, await approval, apply
- Run baseline scenario after each file to confirm no regression
- Each file is independent — can pause between them

## After Phase 1 Step 3: Phase 2 (the reporter)

Once all 13 event types are being emitted, build `report.py` per `REPORT_DECISIONS.md`. The reporter is greenfield code; it reads events.jsonl and produces:
- `current_status.txt` (live state, overwritten weekly)
- `{YYYY}.txt` (monthly rows accumulated per year)
- Simulator end-of-run report (fixed-width, the example provided previously)

After Phase 2 lands, operator sees real reports against the 20-year baseline. **Pause for operator review before Phase 3 (delete IPMS output package).**

## Operator preferences carried forward

- "Clean, simple, elegant, robust" — fix at source, don't layer
- Pause at decision branches, don't run more than ~2 pages ahead
- Operator defers to Claude on coding decisions; principles: "what is most optimal for long term robustness and success of IRAPM"
- Past pattern Claude must avoid: implementing fix before reproducing bug; assumed-not-verified attribute names
- New discipline added this session: **attribute audit before emission diffs** — every `.foo` access and field name verified against source before writing the change

## Cycle 1 of next session

Start by inspecting the events.jsonl produced by the baseline run:

```bash
ls -la C:\portfolio\runs\2005-2025_fd1ad2\_harness_state\
# Expect: events.jsonl present, ~size 50-200KB for 20-year baseline
```

Plausibility checks (counts roughly):
- ~1044 cycle_started + 1044 cycle_completed (weekly, 20 years × 52)
- ~5000 cycle_started + 5000 cycle_completed (daily-token, 6 days/week × ~50 weeks × 20 years; exact count varies by weekend/holiday handling)
- 1044 decision_made
- 1044 portfolio_snapshot
- 240 state_snapshot (monthly heartbeat × 20 years; more once action_layer adds the other three triggers)
- 0 cycle_halted (clean baseline)

Spot-check 2-3 records visually to confirm payload shapes are sensible.

Then proceed to action_layer.py.
